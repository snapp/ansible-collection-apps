---

galaxy_info:
  author: Christopher A. Snapp
  description: >-
    Deploy code-server (VS Code in the browser) as one rootless Podman Quadlet
    per developer, each under its own OS/IdM account, behind a Caddy TLS proxy.
  license: GPL-2.0-or-later
  min_ansible_version: '2.15'
  platforms:
    - name: EL
      versions:
        - '10'
  galaxy_tags:
    - codeserver
    - vscode
    - ide
    - developer
    - podman
    - quadlet

dependencies: []
